# Server Configuration
PORT=5000
NODE_ENV=production

# OpenAI API Configuration (for AI categorization feature)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Database
# In production, this will be stored in a persistent volume
DATABASE_PATH=/app/data/finance.db

# CORS Settings (optional - set if you need specific origins)
# CORS_ORIGIN=https://yourdomain.com
